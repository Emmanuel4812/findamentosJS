const courses = [
    {id: 1, name: 'a'},
    {id: 2, name: 'b'},
];

const course = courses.find (course => course.name === 'a'); //una arrow function es una funcion pero mas simplificada en su manera de escribir


console.log(course);


